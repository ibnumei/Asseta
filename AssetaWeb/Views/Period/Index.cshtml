@model IEnumerable<AssetaWeb.Models.PeriodTbl>
@{
    ViewData["Title"] = "Index";
}

<section class="content-header">
    <h1>
        Period Master
        <small></small>
    </h1>
    <ol class="breadcrumb">
        <li>Master</li>
        <li class="active">Period Master</li>
    </ol>
    <br />
    <p>
        <a asp-action="Create" class="btn btn-default">
            <i class="fa fa-plus-square"></i>
            <span>&nbsp;  Add New Data</span>
        </a>
    </p>
</section>
<!-- Content Header (Page header) -->
<!-- Main content -->
<section class="content container-fluid">
    <div class="box">
        <div class="box-header">
            <h3 class="box-title">All of Data in Period Master</h3>
        </div>
        <!-- /.box-header -->
        <div class="box-body">
            <table id="DataTableSite" class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>Period Id</th>
                        <th>Period Type</th>
                        <th>Days</th>
                        <th></th>
                    </tr>
                </thead>
                @foreach (var item in Model)
                {
                    <tbody>
                        <tr>
                            <td>
                                @Html.DisplayFor(m => item.PeriodId)
                            </td>
                            <td>
                                @Html.DisplayFor(m => item.PeriodType)
                            </td>
                            <td>
                                @Html.DisplayFor(m => item.Days)
                            </td>
                            <td>
                                <a asp-action="Edit" asp-route-Id="@item.PeriodId" class="btn  btn-default">
                                    <i class="fa fa-edit"></i>
                                    <span>Edit</span>
                                </a>&nbsp;

                                <a asp-controller="Site" asp-action="Delete" asp-route-id="@item.PeriodId" class="btn  btn-default">
                                    <i class="fas fa-trash-alt"></i>
                                    <span>Delete</span>
                                    @*<input type="submit" value="Delete" class="btn btn-sm btn-danger" />*@
                                </a>

                            </td>
                        </tr>
                    </tbody>
                }

            </table>
        </div>
        <!-- /.box-body -->
    </div>
</section>

